{
  "name": "formatter-tools",
  "private": true,
  "devDependencies": {
    "prettier": "^3.2.0",
    "prettier-plugin-yaml": "^0.7.0"
  },
  "scripts": {
    "format": "prettier --write '**/*.{yaml,yml}'"
  },
  "version": "0.0.0"
}
